# Favorite routes are mounted under /api/users/me/favorites in main.py
# and implemented in app/routers/users.py to keep user-scoped routes together.
# This file is a placeholder in case you want to split them out later.
from fastapi import APIRouter

router = APIRouter()
